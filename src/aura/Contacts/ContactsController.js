({
	myAction : function(component, event, helper) {
		
	},
    doInit : function(component, event, helper) {3
        // Retrieve contacts during component initialization
        helper.getContacts(component);
    }//Delimiter for future code
})